# 菜谱 Graph RAG 项目讲解

## 项目概述

我基于已有菜谱知识图谱，实现了一个结合文本检索和图检索的问答系统。

Neo4j 保存菜谱、食材、步骤及其关系；Milvus 保存菜谱文本块的向量。用户提出问题后，系统选择混合检索、图检索或组合检索，将证据交给 DeepSeek 生成回答，并标注证据编号。

项目参考了 Datawhale all-in-rag 的 C9。我逐步实现主要模块，并针对实际运行中发现的检索和证据问题进行了修改。

## 为什么使用两种数据库

两种数据库承担不同职责，不需要逐节点一一对应。

Milvus 用于根据问题的语义寻找相关文本；Neo4j 用于查询明确的实体连接和属性条件。

例如，询问椰浆用量时，可以检索菜谱正文；询问青蟹和椰浆通过哪些菜谱关联时，可以查询两种食材之间经过菜谱的路径。

## 为什么切块后还要回填完整菜谱

文本块便于检索局部信息，但可能缺少回答所需的上下文。

例如，核对食材表与步骤中的用量是否一致，需要同时看到两部分。系统先用文本块定位相关菜谱，再按配置回填父文档。

代价是上下文变长，并可能引入无关步骤，因此当前设置了回填数量和正文长度上限。

## 我做过的关键修改

### 1. 给多跳路径增加关系类型过滤

最初检索青蟹与椰浆的连接时，系统返回了经过共同类型节点的路径，其评分还高于真正经过菜谱的路径。

我增加了路径关系类型过滤，让指定REQUIRES的查询只返回满足该关系条件的路径，减少无关结构干扰。

### 2. 将子图转换为实际证据

最初子图正文只有节点数和关系数，模型无法据此回答具体步骤问题。

我补充了节点信息、关系、关系属性以及步骤描述，使生成模块能够使用实际图谱内容回答问题。

### 3. 将难度条件落实到查询中

仅让模型识别“难度不超过3星”还不够，检索代码也必须执行该条件。

我在多跳分支中加入难度过滤，要求路径中的菜谱具有难度值且不超过上限。当前这项过滤还没有覆盖所有检索分支。

### 4. 为回答增加证据编号

我给传入模型的文档编号，并要求关键事实引用对应证据。

例如，组合检索返回图路径和完整菜谱后，回答分别引用路径说明关联菜谱、引用食材表说明椰浆为100ml。

### 5. 清理分类根节点产生的噪声

数据中的“菜谱”根节点也带有Recipe标签，因此曾被生成文档并进入检索结果。

我在文档准备阶段排除该节点，清理其已有向量，并给两个Neo4j兜底查询增加排除条件。原始图谱节点仍然保留。

## 当前结论与限制

目前已经跑通食材用量、步骤顺序、关系路径、难度过滤和组合证据回答等案例。

这些案例说明功能可以执行，但尚不能证明某种策略整体更准确。当前没有完成纯向量基线、固定评测集和消融实验，因此不宣称准确率提升比例。

此外，项目使用已有图谱，没有实现原始文本的自动实体关系抽取；也没有实现多轮对话记忆、自动增量同步或专门的因果推理算法。